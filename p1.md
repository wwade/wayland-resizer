I'd like to generalize this in the config. I think the structure should be something like:

{ "classifiers": {"googleChat":{"class":"chrome|chromium", "title": "^google chat"}}, "resolutions": {"1920x1080": {"googleChat": {"width":, 1920, "centered": true}}}}

This will allow us to more simply change the behaviour without having to reload the extension by logging out again.impl
